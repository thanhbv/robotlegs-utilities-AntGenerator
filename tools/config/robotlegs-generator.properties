####################################### VERSION 1.0 ###################################
Generator.author           = Elad Elrom

################################ TEMPLATE PROPERTIES ##################################
templates.dir			= templates/
robotlegs.templates.dir = templates/robotlegs
robosignals.templates.dir = templates/roboSignals

################################ DIRECTORIES ################################
control.dir               = controller
model.dir                 = model
view.dir                  = view
vo.dir                    = vo
services.dir              = services
events.dir				  = events
signals.dir				  = signals
signaltons.dir			  = signaltons

################################ PROTECTED PROPERTIES #################################
namespace           = ${com.dir}.${domain.dir}.${project.dir}
project.main.url    = ${workspace.dir}/${project.dir}/${src.dir}
project.root.url    = ${workspace.dir}/${project.dir}/
project.url         = ${workspace.dir}/${project.dir}/${src.dir}/${project.path}